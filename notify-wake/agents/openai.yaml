interface:
  display_name: "Notify & Wake"
  short_description: "Design event-driven task wake lifecycles"
  default_prompt: "Use $notify-wake to replace model polling with a durable event-driven wake path for this long-running task."
