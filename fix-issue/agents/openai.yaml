interface:
  display_name: "Fix Issue Workflow"
  short_description: "Reproduce issues, fix them, and verify local changes"
  default_prompt: "Use $fix-issue to read an issue, reproduce bugs, implement fixes, and verify the result without committing or opening a PR unless I explicitly request it."
