interface:
  display_name: "Review Fix Loop"
  short_description: "Iterative review and fix cycles"
  default_prompt: "Use $review-fix-loop to review and fix the current changes; it starts Goal Mode automatically when needed and stops after a clean review or the iteration limit."
policy:
  allow_implicit_invocation: false
