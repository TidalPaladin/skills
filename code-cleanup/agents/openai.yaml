interface:
  display_name: "Code Cleanup"
  short_description: "Simplify code while preserving behavior"
  default_prompt: "Use $code-cleanup to simplify the selected code while preserving behavior."
