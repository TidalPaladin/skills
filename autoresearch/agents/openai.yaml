interface:
  display_name: "Autoresearch"
  short_description: "Run reproducible empirical research studies"
  default_prompt: "Use $autoresearch to run a bounded, reproducible empirical study in this repository. The skill starts Goal Mode when needed and commits and pushes study changes without creating a pull request."
policy:
  allow_implicit_invocation: false
